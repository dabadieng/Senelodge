<nav class="colorNav navbar fixed-top navbar-expand-lg navbar-dark">
	<a class="navbar-brand" href="{{ path('homepage') }}">
		SénéLodge
	</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse colorNav" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('homepage') }}">
					Accueil
					<span class="sr-only">
						(current)
					</span>
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('ad_index') }}">
					Annonces
					<span class="sr-only">
					</span>
				</a>
			</li>
		</ul>
		<ul class="navbar-nav ml-auto colorNav">
			{% if app.user %}
				<li class="nav-item dropdown colorNav">
					<a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
						<img src="{{asset("/upload/image/"~ app.user.picture)}}" class="avatar avatar-mini" alt="Avatar de {{app.user.fullName}}">
						{{app.user.fullName}}
					</a>
					<div class="dropdown-menu dropdown-menu-right colorNav" aria-labelledby="accountDropdownLink">
						<a class="dropdown-item" href="{{path('ads_create')}}">
							Créer une annonce
						</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{path('account_index')}}">
							Mon compte
						</a>
						<a class="dropdown-item" href="{{path('account_booking')}}">
							Mes réservations
						</a>
						<a class="dropdown-item" href="{{path('account_profile')}}">
							Modifier mon profil
						</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{path('account_logout')}}">
							Se déconnecter
						</a>
					</div>
				</li>

			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('account_register') }}">
						Inscription
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('account_login') }}">
						Connexion
					</a>
				</li>
			{% endif %}
		</ul>
	</div>
</nav>

<br>
<br>
<br>
<br>

{% for label, messages in app.flashes %}
	<div class="container">
		<div class="alert-{{ label }}">
			{% for message in messages %}
				<p>
					{{ message|raw }}
				</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
